<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        /* General body styling */
        body {
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            display: flex;
        }

        /* Split container */
        .split-container {
            display: flex;
            width: 100%;
            height: 100%;
        }

        /* Left panel styling */
        .left-panel {
            flex: 1;
            background: linear-gradient(-45deg, #7eecff, #a5eafa, #2ff6f2, #23d5ab);
            background-size: 400% 400%;
            animation: gradientBG 15s ease infinite;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .left-panel::before {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            top: -50px;
            left: -50px;
        }

        .left-panel::after {
            content: '';
            position: absolute;
            width: 200px;
            height: 200px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
            bottom: -80px;
            right: -80px;
        }

        .left-panel button {
            padding: 15px 40px;
            background: transparent;
            border: 2px solid white;
            color: white;
            font-size: 1.2rem;
            border-radius: 30px;
            cursor: pointer;
            transition: all 0.3s ease;
            z-index: 1;
        }

        .left-panel button:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-5px);
        }

        /* Right panel styling */
        .right-panel {
            flex: 1;
            background: white;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Form styling */
        .form-container {
            width: 70%;
            max-width: 400px;
        }

        h1 {
            margin-bottom: 2rem;
            font-size: 2.5rem;
            color: #228cb3;
            text-align: center;
        }

        /* Input styling */
        .input-group {
            margin-bottom: 1.5rem;
            position: relative;
        }

        .input-group input {
            width: 100%;
            padding: 15px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .input-group input:focus {
            border-color: #228cb3;
            box-shadow: 0 0 10px rgba(34, 140, 179, 0.2);
            outline: none;
        }

        /* Submit button */
        .submit-btn {
            width: 100%;
            padding: 15px;
            background: linear-gradient(45deg, #228cb3, #27dfd3);
            border: none;
            border-radius: 8px;
            color: white;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.3s ease;
        }

        .submit-btn:hover {
            transform: translateY(-3px);
        }
        a{
            text-decoration: none;
            color: white;
        }

        /* Animations */
        @keyframes gradientBG {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .split-container {
                flex-direction: column;
            }

            .left-panel {
                padding: 2rem;
                flex: none;
                height: 30vh;
            }

            .right-panel {
                flex: none;
                height: 70vh;
            }

            .form-container {
                width: 90%;
            }
        }
    </style>
</head>
<body>
    <div class="split-container">
        <div class="left-panel">
            <h1>Welcome Back!</h1>
            <button><a href="/signup">Don't have account!!</a></button>
        </div>
        
        <div class="right-panel">
            <div class="form-container">
                <h1>Login</h1>
                <form method="post" action="/user/login">
                    <div class="input-group">
                        <input type="email" name="email" placeholder="Email" required>
                    </div>
                    <div class="input-group">
                        <input type="password" name="password" placeholder="Password" required>
                    </div>
                    <button type="submit" class="submit-btn">Sign In</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>